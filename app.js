// import functions and grab DOM elements
import { myClickHandler, myClickHandler1 } from "./handlers.js"


const addButton = document.getElementById('add-button');

addButton.addEventListener('click', myClickHandler);

const subtractionButton = document.getElementById('sub-button');

subtractionButton.addEventListener('click', myClickHandler1);

















// initialize state

// set event listeners to update state and DOM

